{
  "name": "personal-portfolio-cn",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "cf:whoami": "wrangler whoami"
  },
  "devDependencies": {
    "vercel": "^53.3.0",
    "wrangler": "^4.90.0"
  },
  "dependencies": {
    "nodemailer": "^6.10.0"
  }
}
